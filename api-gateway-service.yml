server:
  port: 9000

zuul:
  ignoredServices: '*'
    host:
      connect-timeout-millis: 20000
      socket-timeout-millis: 20000
  routes:
    stripPrefix: false
    trade-service:
        path: /trade
        serviceId: trade-service
        stripPrefix: false
